<%= render partial: 'flash' %>
<div class="page nobg">
  <h1 class="title">
    <%= @group.name %>
  </h1>

  <h2>Members</h2>
  <% @members.each do |member| %>
    <%= member.username %>
    <%= member.first_name %>
    <%= member.last_name %>
  <% end %>
</div>
