<%= form_for @group_member do |f| %>
  <%= f.hidden_field :group_id, value: @group_member.group.id %>
  <%= f.collection_select(:user_id, User.all, :id, :username, { prompt: 'Please select a username' }) %>
  <%= f.submit 'Add Member', class: 'button primary radius' %>
<% end %>
